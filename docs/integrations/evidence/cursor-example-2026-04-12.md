# Cursor MCP Example Status — 2026-04-12

- Host: Cursor
- Status: example-only
- Source doc: https://cursor.com/docs/mcp

## Why example-only

We have a copy-pasteable config example for Cursor, but we have not yet completed a committed end-to-end validation run in a real Cursor host session.

## Promotion criteria

Promote Cursor to `validated` only after capturing:

1. config acceptance in a real Cursor environment,
2. startup proof,
3. `tools/list` visibility,
4. at least one successful Signal Graph tool interaction,
5. a dated evidence artifact.
